{
    "name": "${localWorkspaceFolderBasename}",
    "image": "ghcr.io/thezhe/devimagepp:main",
    "postStartCommand": "rm -rf test/**/build" // Conflicts with "brobeson.vscode-cmake-lint" startup
}
